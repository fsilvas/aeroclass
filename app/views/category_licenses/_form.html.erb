<%= form_for(category_license) do |f| %>
  <% if category_license.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category_license.errors.count, "error") %> prohibited this category_license from being saved:</h2>

      <ul>
      <% category_license.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :category_id %>
    <%= f.number_field :category_id %>
  </div>

  <div class="field">
    <%= f.label :license_id %>
    <%= f.number_field :license_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
